# 概念数据模型

> **阶段定位**：系统设计阶段产出。这是**与数据库无关**的抽象层，关注实体和关系。
> 物理模型（表结构、字段类型）在详细设计阶段的 `DATA_MODEL_PHYSICAL.md` 里。

---

## 实体识别

| 实体名 | 中文名 | 一句话描述 |
|--------|--------|-----------|
| User | 用户 | |
| [Entity] | | |

---

## 实体关系图

```
User ──1:N──> [Entity A]
[Entity A] ──M:N──> [Entity B]
[Entity B] ──1:1──> [Entity C]
```

**关系说明**：

- `User → [Entity A]`（1:N）：一个用户可以有多个...，一个...只属于一个用户
- `[Entity A] ↔ [Entity B]`（M:N）：

---

## 核心实体详情

### User（用户）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | UUID | 主键 |
| email | String | 登录用 |
| created_at | DateTime | |

### [Entity A]

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | UUID | 主键 |
| user_id | UUID | 关联 User |
| | | |

---

## 数据量估算

| 实体 | 初期预估 | 一年后预估 | 说明 |
|------|---------|-----------|------|
| User | | | |
| [Entity A] | | | |

---

> 注：具体的表结构、索引设计在详细设计阶段的 `DATA_MODEL_PHYSICAL.md` 中完成。
